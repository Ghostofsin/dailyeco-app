"use server";

import { getResultByUser, getUserByEmail } from "../methods";

export async function getUsersResults(email) {
  
  // console.log("user", user);

  // const userId = user.parse;
  console.log("userId", userId);

  
  console.log("result", result);

  return result;
}
